#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: success (0)
 */
int main(void)
{
	/* Declare an array of four integers */
	int arr[4];

	/* Add elements */
	arr[0] = 0;
	arr[1] = 10;
	arr[2] = 20;
	arr[3] = 30;
	arr[4] = 40;

	printf("Index 0 output is: %d", arr[0]);

	return (0);
}
