#include <stdio.h>
/**
 * main - Entry point.
 *
 * Return: success (0).
 */
int main(void)
{
	int age = 20; /* An int variable */

	printf("%d", age); /*  Outputs the value of age (20) */
	printf("%p", &age); /* Ouputs the memory address */
	return (0);
}
