#include <stdio.h>
/**
 * main - Entry point, do...while loop.
 *
 * Return: success (0).
 */
int main(void)
{
	int i = 0;

	do {
	printf("Output is: %d\n", i);
	i++;
	} while (i  < 5);

	return (0);
}
