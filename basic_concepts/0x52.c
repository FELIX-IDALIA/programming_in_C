#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: success (0).
 */
int main(void)
{
	char alphabets[] = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
	int len = strlen(alphabets), size = sizeof(alphabets);
i
	printf("The length of alphabets is: %d\n", len);
	printf("The size of alphebets is:   %d", size);
	return (0);
}
